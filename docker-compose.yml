services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: success-academy-postgres
    environment:
      POSTGRES_DB: success_academy_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"  # Using 5433 on host to avoid conflict with existing PostgreSQL
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - academy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API Server (Development)
  api-dev:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: success-academy-api-dev
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=5000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=success_academy_db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - CORS_ORIGIN=http://localhost:5173
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - academy-network
    restart: unless-stopped
    command: sh -c "npm install && npm run migrate && npm run dev"
    profiles:
      - dev

  # Backend API Server (Production)
  api-prod:
    build:
      context: ./server
      dockerfile: Dockerfile.prod
    container_name: success-academy-api-prod
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=success_academy_db
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - CORS_ORIGIN=http://localhost
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - academy-network
    restart: unless-stopped
    command: sh -c "npm run migrate && npm start"
    profiles:
      - production

  # Frontend Development Server
  app-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: success-academy-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://localhost:5000/api
    networks:
      - academy-network
    restart: unless-stopped
    profiles:
      - dev

  # Frontend Production Service
  app-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: success-academy-prod
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=http://localhost:5000/api
    networks:
      - academy-network
    restart: unless-stopped
    depends_on:
      - api-prod
    profiles:
      - production

  # pgAdmin 4 - Database Management Tool (Development)
  pgadmin-dev:
    image: dpage/pgadmin4:latest
    container_name: success-academy-pgadmin-dev
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    ports:
      - "8888:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - academy-network
    restart: unless-stopped
    depends_on:
      - postgres
    profiles:
      - dev

  # pgAdmin 4 - Database Management Tool (Production)
  pgadmin-prod:
    image: dpage/pgadmin4:latest
    container_name: success-academy-pgadmin-prod
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    ports:
      - "8889:80"
    volumes:
      - pgadmin_data_prod:/var/lib/pgadmin
    networks:
      - academy-network
    restart: unless-stopped
    depends_on:
      - postgres
    profiles:
      - production

volumes:
  postgres_data:
  pgadmin_data:
  pgadmin_data_prod:

networks:
  academy-network:
    driver: bridge


